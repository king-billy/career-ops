---
description: Generate application bundle (CV + cover letter + open-ended answers)
---

Generate an application bundle (CV PDF + cover letter PDF + open-ended answers) using career-ops apply mode. Use this alongside Simplify (or similar autofill tools) — this mode does not touch structured fields. Report number or JD context:

$ARGUMENTS

Load the career-ops skill:
```
skill({ name: "career-ops" })
```
